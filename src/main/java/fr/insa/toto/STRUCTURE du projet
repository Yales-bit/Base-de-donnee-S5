Dans un souci d'organisation je propose qu'on ne mette que constructeurs/attributs/méthodes toString (pour le débuggage) 
dans les classes du dossier model.

Les méthodes de logique métier (ex :Vérifier que le joueur n'existe pas déjà, 
valider l'âge, mettre à jour le score total, créer la logique d'affectation aléatoire aux équipes.) iront dans le dossier Services.

Les méthodes coté BDD mysql/ Accès aux données iront dans les classes du dossier "acces" : Écrire la requête SQL INSERT INTO JOUEUR, 
convertir la ligne de la base de données en objet Joueur.java (mapping), gérer la connexion à la base.

Et tous ce qui est interface graphique dans webui.

Cette organisation rend le projet plus clair, et plus facile à maintenir.